/**
 * Content Script
 * Runs in the context of web pages
 * Currently minimal - most functionality is handled via chrome.scripting API
 */

// Notify that content script is loaded
console.log('[Chrome Gemini Sync] Content script loaded');
